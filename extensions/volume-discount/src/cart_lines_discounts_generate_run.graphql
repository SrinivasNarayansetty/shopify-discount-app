query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
          product {
            id
          }
        }
      }
    }
  }
discount {
    metafield(namespace: "volume_discount", key: "rules") {
      value
    }
  }
}